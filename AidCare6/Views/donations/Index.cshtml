@model IEnumerable<AidCare6.Models.donations>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DonationsDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.donationsAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DonationsDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.donationsAmount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.donationsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.donationsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.donationsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
